---
- hosts: tag_Name_forum1_stage
  sudo: yes
  vars:
    secure_dir: ../../../configuration-secure/ansible
    local_dir: ../../../configuration-secure/ansible/local
  vars_files:
    - "{{secure_dir}}/vars/stage/forum.yml"
  roles:
    - common
    - oraclejdk
    - elasticsearch
    - role: nginx
      nginx_sites:
        - forum
    - forum
